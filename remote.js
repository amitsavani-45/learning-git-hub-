/*Definition

git remote is a Git command used to manage connections between your local repository and a remote repository (usually hosted on platforms like GitHub).

A remote repository is a version of your project stored on the internet or a server that allows collaboration and backup.

 Purpose of git remote

The main purpose of git remote is to:

Connect a local repository to a remote repository

View remote repository details

Add or remove remote connections

Rename remote repositories

It helps in synchronizing local work with online repositories.

 What is a Remote?

A remote is simply a reference (shortcut name) for a repository hosted elsewhere.

By default, when you clone a repository, Git automatically creates a remote named:

origin


origin is just a default name — it represents the URL of the remote repository.
*/